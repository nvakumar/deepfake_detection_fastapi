# Core Web Framework
fastapi
uvicorn
python-multipart

# Templating/Frontend (if used)
jinja2

# Image Processing
pillow
numpy

# --- CPU Optimization for PyTorch ---
# 1. Correctly include the extra index URL. This must be a direct URL,
#    not a search query, and it should point to the correct PyTorch index.
#    The link below is the current correct index for CPU wheels.
--extra-index-url https://download.pytorch.org/whl/cpu

# 2. Specify PyTorch and TorchVision versions.
#    Note: Given your error, I am using the required Python 3.13 versions.
#    If 2.5.1 is not available yet, this will install the highest compatible version.
torch>=2.5.1
torchvision>=0.20.1
